/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['trustwallet.com'], // Add trustwallet.com to the allowed domains
      },
};

export default nextConfig;
